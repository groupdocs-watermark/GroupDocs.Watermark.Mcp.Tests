name: integration

on:
  push:
    branches: [master, main]
  pull_request:
    branches: [master, main]
  schedule:
    # Nightly — catches regressions introduced by nuget.org / dnx / runtime changes.
    - cron: "0 6 * * *"
  workflow_dispatch:
    inputs:
      package_version:
        description: "GroupDocs.Watermark.Mcp version on nuget.org to test"
        required: false
        default: "26.9.0"
  # Triggered from the main repo's publish workflow right after a NuGet push.
  # Payload: { "package_version": "x.y.z" }
  repository_dispatch:
    types: [nuget-published]

jobs:
  test:
    name: ${{ matrix.os }} · dnx
    runs-on: ${{ matrix.os }}
    # A hung run must fail fast: macOS jobs were hanging in 'Integration tests'
    # and getting killed at ~45 min with NO logs uploaded (BlobNotFound).
    timeout-minutes: 20
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    env:
      MCP_PACKAGE_VERSION: >-
        ${{ github.event.inputs.package_version
            || github.event.client_payload.package_version
            || '26.9.0' }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      # GroupDocs.Watermark's PDF and Diagram content paths call System.Drawing
      # internally (Font / Color / FontFamily / FontStyle on text-watermark
      # rendering). Linux runners need:
      # - libgdiplus           : managed System.Drawing implementation
      # - libfontconfig1       : font discovery
      # - ttf-mscorefonts-installer : Arial / Times New Roman / etc. — required
      #   for text-watermark glyph rendering. Without these, AddWatermark fails
      #   on font resolution and the wrapped error reads "An error occurred
      #   invoking 'add_watermark'." Pattern lifted from the sibling python-
      #   conversion Dockerfile. EULA accepted via debconf-set-selections;
      #   fc-cache refresh post-install.
      # Note: the .NET Docker base aspnet:10.0 inherits these fonts from
      # Debian-bookworm-slim; bare ubuntu-24.04 runners do not.
      - name: Install Linux native deps
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          echo "ttf-mscorefonts-installer msttcorefonts/accepted-mscorefonts-eula select true" | sudo debconf-set-selections
          sudo apt-get install -y --no-install-recommends \
            libgdiplus libfontconfig1 ttf-mscorefonts-installer
          # Refresh font cache so newly-installed MS fonts are discoverable.
          sudo fc-cache -f -v > /dev/null

      # macOS runners ship neither GDI+ nor libgdiplus. GroupDocs engines that
      # touch image / spreadsheet content P/Invoke System.Drawing, which loads
      # libgdiplus. The dnx-spawned server's dyld does NOT search the Homebrew
      # prefix or /usr/local/lib (a failing run showed its dlopen only probes the
      # package dir, the .NET shared-framework dir, and /usr/lib). So install via
      # Homebrew and copy the dylib into the .NET runtime's shared-framework dir,
      # a path .NET's System.Drawing explicitly probes; libgdiplus's own deps
      # (cairo, glib, …) stay resolvable via the absolute Homebrew paths baked
      # into its load commands. (Windows has GDI+ built in; Linux: apt above.)
      - name: Install macOS native deps
        if: runner.os == 'macOS'
        run: |
          set -euxo pipefail
          brew install mono-libgdiplus
          GDIP="$(brew --prefix mono-libgdiplus)/lib/libgdiplus.dylib"
          otool -L "$GDIP" || true
          for d in "$HOME/.dotnet/shared/Microsoft.NETCore.App/"*/ ; do
            cp -f "$GDIP" "$d"
            echo "installed libgdiplus -> $d"
          done

      - name: Show environment
        shell: bash
        run: |
          dotnet --info
          echo "Testing GroupDocs.Watermark.Mcp@${MCP_PACKAGE_VERSION}"
          dnx --help || true

      - name: Write GroupDocs license (optional)
        # If the repo defines a GROUPDOCS_LICENSE secret (base64 of a .lic file),
        # write it to disk and point GROUPDOCS_LICENSE_PATH at it. Unlocks the
        # licensed-mode AddWatermark tests; evaluation-mode tests still run.
        env:
          GROUPDOCS_LICENSE: ${{ secrets.GROUPDOCS_LICENSE }}
        if: env.GROUPDOCS_LICENSE != ''
        shell: bash
        run: |
          echo "$GROUPDOCS_LICENSE" | base64 -d > "$RUNNER_TEMP/GroupDocs.lic"
          echo "GROUPDOCS_LICENSE_PATH=$RUNNER_TEMP/GroupDocs.lic" >> "$GITHUB_ENV"

      # Pre-warm the dnx package cache AND surface the server's launch stderr.
      # With a COLD dnx cache the fixture's in-pipe launch of a large package can fail
      # ConnectAsync in ~2s ('server shut down unexpectedly') before the download
      # completes - proven on Signature@26.9.0 (3 consecutive red runs cold, 42/42
      # green after warm-up). This step downloads/launches the server once
      # (stdin=/dev/null -> reads EOF -> clean exit 0; 124 = killed by timeout = also
      # healthy) and prints stderr, which the fixture error hides.
      - name: Pre-warm dnx package cache (+ launch diagnostics)
        shell: bash
        run: |
          set +e
          DNX=dnx
          [ "$RUNNER_OS" = "Windows" ] && DNX=dnx.cmd
          timeout 180 "$DNX" GroupDocs.Watermark.Mcp@${MCP_PACKAGE_VERSION} --yes < /dev/null > dnx_probe_out.log 2> dnx_probe_err.log
          code=$?
          echo "probe exit=$code (0 or 124 = healthy; 127 = dnx not found; else see stderr)"
          echo "--- stderr ---"; cat dnx_probe_err.log
          echo "--- stdout (head) ---"; head -5 dnx_probe_out.log
          exit 0

      - name: Restore
        run: dotnet restore

      - name: Build
        run: dotnet build --configuration Release --no-restore

      # Category=Metered is excluded on purpose: those tests bill a real metered
      # account per operation, so they run only from the dedicated `metered` workflow.
      - name: Integration tests
        run: >-
          dotnet test --configuration Release --no-build
          --filter "Category!=Metered"
          --logger "trx;LogFileName=integration.trx"
          --logger "console;verbosity=detailed"
          --blame-hang --blame-hang-timeout 5m --blame-hang-dump-type full

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: trx-${{ matrix.os }}
          path: |
            **/TestResults/*.trx
            **/TestResults/**/*.dmp
            **/TestResults/**/Sequence_*.xml
          if-no-files-found: ignore
